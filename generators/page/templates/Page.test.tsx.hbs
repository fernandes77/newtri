import { render, screen } from 'utils/testHelpers'
import { {{pascalCase name}} } from '.'

describe('Page <{{pascalCase name}} />', () => {
  it('should ', () => {
    render(<{{pascalCase name}} />)
  })
})
