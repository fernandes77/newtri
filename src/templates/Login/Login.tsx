import { Center } from '@mantine/core'
import { LoginCard } from 'components/LoginCard'

export const Login = () => {
  return (
    <Center sx={{ height: '100vh' }}>
      <LoginCard />
    </Center>
  )
}
